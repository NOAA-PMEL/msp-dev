apiVersion: v1
kind: Service
metadata:
  name: sampling-system
  namespace: msp-system
  labels:
    app: sampling-system
spec:
  selector:
    app: sampling-system
  ports:
  - name: http
    protocol: TCP
    # port: 8000
    port: 80
    # targetPort: 8000
    targetPort: 8080
  type: ClusterIP  